{
	"scripts": {
		"test": "deno test -A --unstable --import-map=import_map.json ./src/test/",
		"compile": "deno run -A --unstable --import-map=import_map.json ./src/build.ts",
		"app": "deno run -A --unstable --import-map=import_map.json ./src/main.ts"
	}
}
